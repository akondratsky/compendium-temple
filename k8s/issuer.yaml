apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: compendium-issuer
  namespace: default
spec:
  acme:
    email: aleksandrkondratskii@gmail.com
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: compendium-certs-secret
    solvers:
    - http01:
        ingress:
          name: compendium-ingress

