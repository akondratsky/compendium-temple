apiVersion: v1
kind: PersistentVolume
metadata:
  name: ct-persistent-volume
spec:
  capacity:
    storage: 80Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    # security is in my nature
    path: /root/database
